export default function NotFound (){
    return(
        <div class="alert alert-danger" style={{marginTop: "100px", width: "30%", marginLeft: "35%"}} role="alert">
            Hmm… we can’t seem to find that page.
        </div>
    )
}